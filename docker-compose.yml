services:
  spell-crafter:
    build: .
    container_name: spell-crafter
    ports:
      - "4173:4173"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
